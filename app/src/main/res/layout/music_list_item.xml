<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:id="@+id/musicListItem"
    android:layout_marginTop="10dp"
    android:outlineSpotShadowColor="@color/grayLine"
    android:outlineAmbientShadowColor="@color/grayLine">

    <TextView
        android:id="@+id/musicName"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="10dp"
        android:layout_gravity="center"
        android:text="name"
        android:textSize="15dp"/>

    <TextView
        android:id="@+id/musicTotalTime"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:layout_marginLeft="250dp"
        android:layout_marginTop="10dp"
        android:text="totalTime"
        android:textColor="@color/musicTime"
        android:textSize="15dp"/>

</LinearLayout>